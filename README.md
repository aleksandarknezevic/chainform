# ChainForm

Infrastructure as Code for blockchain protocols.

ChainForm lets protocol teams define desired on-chain state in code, detect configuration drift, and generate safe, reviewable operations before execution.
