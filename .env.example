# Copy to .env and fill in. Referenced from configuration via env("RPC_URL").
# .env is gitignored — never commit real endpoints or keys.
#
# Mainnet example: docs/mainnet-example.md
#   chainform show -f examples/mainnet.hcl
RPC_URL=https://ethereum.publicnode.com
